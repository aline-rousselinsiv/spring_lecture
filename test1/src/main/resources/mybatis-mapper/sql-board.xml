<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">
	<select id="boardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT B.*, TO_CHAR(CDATETIME, 'YYYY-MM-DD') AS CDATE
		FROM TBL_BOARD B
		WHERE 1=1
		<if test="kind != '' and kind != null">
			AND KIND = #{kind}
		</if>
		<if test="sort == 1">
			ORDER BY BOARDNO ASC
		</if>
		<if test="sort == 2">
			ORDER BY TITLE ASC
		</if>
		<if test="sort == 3">
			ORDER BY CNT DESC
		</if>
	</select>
	<select id="boardAdd" parameterType="hashmap" resultType="com.example.test1.model.Board">
		INSERT INTO TBL_BOARD (BOARDNO, TITLE, USERID, CONTENTS)
		VALUES(BOARD_SEQ.NEXTVAL, #{title}, #{userid}, #{contents})
	</select>
	
	<delete id="deleteBoard" parameterType="hashmap">
		DELETE FROM TBL_BOARD
		WHERE BOARDNO = #{boardno}
	</delete>
	
</mapper>




